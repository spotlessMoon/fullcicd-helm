apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "api2.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "api2.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "api2.name" . }}
    spec:
      containers:
        - name: api2
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8001
